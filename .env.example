# Azure App Service context (optional - can also use set_context tool)
AZURE_SUBSCRIPTION_ID=your-subscription-id
AZURE_RESOURCE_GROUP=your-resource-group
AZURE_APP_NAME=your-app-name

# Optional: Service principal auth (if not using az login)
# AZURE_CLIENT_ID=
# AZURE_CLIENT_SECRET=
# AZURE_TENANT_ID=

# Server mode
# HTTP_MODE=true  # Uncomment to run as HTTP server instead of stdio
# PORT=3000
